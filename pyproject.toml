[project]
name = "mcp-test"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "httpx",
    "mcp[cli]>=1.8.0",
]

[tool.uv.workspace]
members = [
    "mcp_server_baidu_maps",
]
